# 数据规则映射模块功能测试验证文档

## 一、测试概述

### 1.1 测试目标
验证 `gnosis-datamapping` 模块的5个核心场景功能：
1. 字段直接匹配（含类型转换和校验）
2. 根节点→指定节点映射
3. 指定节点→根节点映射
4. List数据提取转换
5. String转List+链式规则处理

### 1.2 测试环境
- 后端端口：8086
- API基础路径：`/datamapping`
- 数据库：openGauss 3.0.0
- 前端端口：3003

### 1.3 启动方式
```bash
# 后端启动
cd c:\works\project\ruoyi-vue-pro-self-demo\gnosis-project\gnosis-datamapping
mvn spring-boot:run

# 前端启动
cd frontend
npm install
npm run dev
```

---

## 二、场景1：字段直接匹配

### 2.1 功能说明
- 按字段名直接匹配源字段和目标字段
- 支持类型映射：`'1'`→Integer成功，`'a'`→Integer失败但可降级
- 支持校验：必填、长度、正则、枚举、范围等

### 2.2 配置JSON
```json
{
  "configMetadata": {
    "systemId": "ERP_CONTRACT",
    "systemName": "ERP合同系统",
    "configVersion": "1.0"
  },
  "apiConfig": {
    "apiVersion": "v1.0",
    "targetEndpoint": "/api/v1/contracts/create",
    "jsonRootPath": "$.contractData"
  },
  "directMappings": [
    {
      "sourceField": "contractNo",
      "targetField": "contractNo",
      "dataType": "string",
      "required": true,
      "validationRules": [
        {"type": "required", "message": "合同编号不能为空"},
        {"type": "maxLength", "value": 50, "message": "合同编号长度不能超过50"},
        {"type": "pattern", "value": "^[A-Z0-9_-]+$", "message": "合同编号只能包含大写字母、数字、下划线和横线"}
      ],
      "transforms": ["trim", "uppercase"]
    },
    {
      "sourceField": "contractAmount",
      "targetField": "amount",
      "dataType": "decimal",
      "required": true,
      "validationRules": [
        {"type": "required"},
        {"type": "min", "value": 0.01}
      ],
      "transforms": ["round(2)"]
    },
    {
      "sourceField": "contractStatus",
      "targetField": "status",
      "dataType": "string",
      "required": true,
      "validationRules": [
        {"type": "enum", "values": ["DRAFT", "ACTIVE", "COMPLETED", "CANCELLED"]}
      ],
      "transforms": ["statusMapping"]
    }
  ],
  "transformRules": {
    "statusMapping": {
      "type": "lookup",
      "mapping": {
        "草稿": "DRAFT",
        "生效": "ACTIVE",
        "完成": "COMPLETED"
      },
      "defaultValue": "DRAFT",
      "ignoreCase": true
    },
    "trim": {"type": "function", "function": "String.trim()"},
    "uppercase": {"type": "function", "function": "String.toUpperCase()"},
    "round": {"type": "function", "function": "Math.round(value, precision)", "parameters": {"precision": 2}}
  }
}
```

### 2.3 测试用例

#### 测试1.1：正常转换
**请求：**
```bash
curl -X POST http://localhost:8086/datamapping/config/test \
  -H "Content-Type: application/json" \
  -d '{
    "configId": "config_001",
    "requestJson": "{\"contractData\": {\"contractNo\": \" ht2024001 \", \"contractAmount\": 1500000.567, \"contractStatus\": \"生效\"}}"
  }'
```

**预期响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "success": true,
    "resultData": {
      "contractNo": "HT2024001",
      "amount": 1500000.57,
      "status": "ACTIVE"
    },
    "errors": [],
    "validationErrors": [],
    "executionTime": 15
  }
}
```

#### 测试1.2：校验失败
**请求：**
```bash
curl -X POST http://localhost:8086/datamapping/config/test \
  -H "Content-Type: application/json" \
  -d '{
    "configId": "config_001",
    "requestJson": "{\"contractData\": {\"contractNo\": \"ht-2024-001\", \"contractAmount\": -100, \"contractStatus\": \"审批中\"}}"
  }'
```

**预期响应：**
```json
{
  "code": 200,
  "data": {
    "success": false,
    "resultData": {
      "contractNo": "HT-2024-001",
      "amount": -100,
      "status": "审批中"
    },
    "errors": [],
    "validationErrors": [
      {
        "field": "contractNo",
        "rule": "pattern",
        "message": "合同编号只能包含大写字母、数字、下划线和横线",
        "value": "HT-2024-001"
      },
      {
        "field": "contractAmount",
        "rule": "min",
        "message": "合同金额必须大于0",
        "value": -100
      },
      {
        "field": "contractStatus",
        "rule": "enum",
        "message": "值不在枚举范围内",
        "value": "审批中"
      }
    ]
  }
}
```

#### 测试1.3：类型转换测试
**配置：**
```json
{
  "apiConfig": {"jsonRootPath": "$.sourceData"},
  "directMappings": [
    {
      "sourceField": "age",
      "targetField": "age",
      "dataType": "integer",
      "required": true
    },
    {
      "sourceField": "score",
      "targetField": "score",
      "dataType": "decimal"
    }
  ]
}
```

**成功示例：** `{"sourceData": {"age": "25", "score": "95.5"}}` → `{"age": 25, "score": 95.5}`

**失败降级示例：** `{"sourceData": {"age": "abc", "score": "xyz"}}` → `{"age": "abc", "score": "xyz"}` + 转换错误记录

---

## 三、场景2：根节点→指定节点

### 3.1 功能说明
将根节点下的字段映射到目标JSON指定节点下的字段

### 3.2 配置JSON
```json
{
  "apiConfig": {"jsonRootPath": "$"},
  "crossNodeMappings": [
    {
      "direction": "rootToNode",
      "sourcePath": "$.contractNo",
      "targetPath": "$.basicInfo.contractNo",
      "dataType": "string",
      "transforms": ["trim"]
    },
    {
      "direction": "rootToNode",
      "sourcePath": "$.contractAmount",
      "targetPath": "$.paymentInfo.amount",
      "dataType": "decimal",
      "transforms": ["round(2)"]
    }
  ]
}
```

### 3.3 测试用例

**请求：**
```bash
curl -X POST http://localhost:8086/datamapping/config/test \
  -H "Content-Type: application/json" \
  -d '{
    "configId": "config_002",
    "requestJson": "{\"contractNo\": \"HT2024001\", \"contractAmount\": 1500000.567}"
  }'
```

**预期响应：**
```json
{
  "success": true,
  "resultData": {
    "basicInfo": {
      "contractNo": "HT2024001"
    },
    "paymentInfo": {
      "amount": 1500000.57
    }
  },
  "errors": [],
  "validationErrors": []
}
```

---

## 四、场景3：指定节点→根节点

### 4.1 功能说明
将指定节点下的字段映射到根节点字段

### 4.2 配置JSON
```json
{
  "apiConfig": {"jsonRootPath": "$"},
  "crossNodeMappings": [
    {
      "direction": "nodeToRoot",
      "sourcePath": "$.basicInfo.contractName",
      "targetPath": "$.contractName",
      "dataType": "string"
    },
    {
      "direction": "nodeToRoot",
      "sourcePath": "$.paymentInfo.totalAmount",
      "targetPath": "$.totalAmount",
      "dataType": "decimal",
      "transforms": ["round(2)"]
    },
    {
      "direction": "nodeToRoot",
      "sourcePath": "$.parties[0].partyName",
      "targetPath": "$.principalParty",
      "dataType": "string"
    }
  ]
}
```

### 4.3 测试用例

**请求：**
```bash
curl -X POST http://localhost:8086/datamapping/config/test \
  -H "Content-Type: application/json" \
  -d '{
    "configId": "config_003",
    "requestJson": "{\"basicInfo\": {\"contractName\": \"2024年度采购框架合同\"}, \"paymentInfo\": {\"totalAmount\": 1650000.00}, \"parties\": [{\"partyName\": \"XX科技有限公司\"}]}"
  }'
```

**预期响应：**
```json
{
  "success": true,
  "resultData": {
    "contractName": "2024年度采购框架合同",
    "totalAmount": 1650000.00,
    "principalParty": "XX科技有限公司"
  },
  "errors": [],
  "validationErrors": []
}
```

---

## 五、场景4：List数据提取转换

### 5.1 功能说明
- 按索引提取（取第n条）
- 按条件筛选
- 值拼接（按指定字符分隔）
- 过滤转换

### 5.2 配置JSON
```json
{
  "apiConfig": {"jsonRootPath": "$.contractData"},
  "listExtractionMappings": [
    {
      "type": "extractByIndex",
      "sourcePath": "$.signatories[*]",
      "targetPath": "$.primaryParty",
      "index": 0,
      "itemMappings": [
        {"source": "$.partyName", "target": "$.partyName", "dataType": "string"},
        {"source": "$.partyType", "target": "$.partyType", "dataType": "string", "transforms": ["partyTypeMapping"]}
      ]
    },
    {
      "type": "concatenateValues",
      "sourcePath": "$.signatories[*].partyName",
      "targetPath": "$.allPartyNames",
      "separator": "、"
    },
    {
      "type": "extractByCondition",
      "sourcePath": "$.signatories[*]",
      "targetPath": "$.principalParty",
      "condition": "$.partyType == '甲方'",
      "takeFirst": true
    }
  ],
  "transformRules": {
    "partyTypeMapping": {
      "type": "lookup",
      "mapping": {"甲方": "PRINCIPAL", "乙方": "VENDOR"},
      "defaultValue": "OTHER"
    }
  }
}
```

### 5.3 测试用例

**请求：**
```bash
curl -X POST http://localhost:8086/datamapping/config/test \
  -H "Content-Type: application/json" \
  -d '{
    "configId": "config_004",
    "requestJson": "{\"contractData\": {\"signatories\": [{\"partyName\": \"XX科技\", \"partyType\": \"甲方\"}, {\"partyName\": \"YY供应商\", \"partyType\": \"乙方\"}]}}"
  }'
```

**预期响应：**
```json
{
  "success": true,
  "resultData": {
    "primaryParty": {
      "partyName": "XX科技",
      "partyType": "PRINCIPAL"
    },
    "allPartyNames": "XX科技、YY供应商",
    "principalParty": {
      "partyName": "XX科技",
      "partyType": "PRINCIPAL"
    }
  },
  "errors": [],
  "validationErrors": []
}
```

---

## 六、场景5：String转List+链式规则

### 6.1 功能说明
- String按分隔符拆分为List
- List后续还能再匹配规则
- 链式处理：转换→增强→格式化

### 6.2 配置JSON
```json
{
  "apiConfig": {"jsonRootPath": "$.contractData"},
  "stringToListMappings": [
    {
      "type": "stringSplitToList",
      "sourcePath": "$.partyNames",
      "targetPath": "$.parties",
      "separator": ",",
      "trimItems": true,
      "removeEmpty": true,
      "chainedMappings": [
        {
          "type": "enrichListItems",
          "enrichmentSource": "partyInfoMapping",
          "keyField": "itemValue",
          "targetFields": ["partyType", "creditCode"]
        }
      ]
    },
    {
      "type": "stringSplitToList",
      "sourcePath": "$.contactPhones",
      "targetPath": "$.contacts",
      "separator": ";",
      "trimItems": true,
      "chainedMappings": [
        {
          "type": "transformListItems",
          "itemTransforms": ["phoneFormat"]
        },
        {
          "type": "convertListToObjects",
          "objectTemplate": {
            "phone": "{itemValue}",
            "type": "MOBILE"
          }
        }
      ]
    }
  ],
  "enrichmentMappings": {
    "partyInfoMapping": {
      "type": "lookup",
      "dataSource": "partyInfo",
      "keyField": "partyName",
      "mappingFields": {"partyType": "partyType", "creditCode": "creditCode"},
      "defaultValues": {"partyType": "OTHER", "creditCode": ""}
    }
  },
  "dataSources": {
    "partyInfo": {
      "type": "static",
      "staticData": {
        "XX科技": {"partyType": "PRINCIPAL", "creditCode": "91110108MA1234567X"},
        "YY供应商": {"partyType": "VENDOR", "creditCode": "91110108MA7654321Y"}
      }
    }
  },
  "transformRules": {
    "phoneFormat": {
      "type": "regex",
      "pattern": "(\\d{3})\\d{4}(\\d{4})",
      "replacement": "$1****$2"
    }
  }
}
```

### 6.3 测试用例

**请求：**
```bash
curl -X POST http://localhost:8086/datamapping/config/test \
  -H "Content-Type: application/json" \
  -d '{
    "configId": "config_005",
    "requestJson": "{\"contractData\": {\"partyNames\": \"XX科技, YY供应商, 未知公司\", \"contactPhones\": \"13800138000; 13900139000\"}}"
  }'
```

**预期响应：**
```json
{
  "success": true,
  "resultData": {
    "parties": [
      {
        "partyName": "XX科技",
        "partyType": "PRINCIPAL",
        "creditCode": "91110108MA1234567X"
      },
      {
        "partyName": "YY供应商",
        "partyType": "VENDOR",
        "creditCode": "91110108MA7654321Y"
      },
      {
        "partyName": "未知公司",
        "partyType": "OTHER",
        "creditCode": ""
      }
    ],
    "contacts": [
      {"phone": "138****8000", "type": "MOBILE"},
      {"phone": "139****9000", "type": "MOBILE"}
    ]
  },
  "errors": [],
  "validationErrors": []
}
```

---

## 七、Postman Collection

导入以下JSON到Postman：

```json
{
  "info": {
    "name": "数据规则映射模块API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "场景1-正常转换",
      "request": {
        "method": "POST",
        "header": [{"key": "Content-Type", "value": "application/json"}],
        "body": {
          "mode": "raw",
          "raw": "{\"configId\": \"config_001\", \"requestJson\": \"{\\\"contractData\\\": {\\\"contractNo\\\": \\\" ht2024001 \\\", \\\"contractAmount\\\": 1500000.567, \\\"contractStatus\\\": \\\"生效\\\"}}\"}"
        },
        "url": {"raw": "http://localhost:8086/datamapping/config/test", "host": ["localhost"], "port": "8086", "path": ["datamapping", "config", "test"]}
      }
    },
    {
      "name": "场景1-校验失败",
      "request": {
        "method": "POST",
        "header": [{"key": "Content-Type", "value": "application/json"}],
        "body": {
          "mode": "raw",
          "raw": "{\"configId\": \"config_001\", \"requestJson\": \"{\\\"contractData\\\": {\\\"contractNo\\\": \\\"ht-2024-001\\\", \\\"contractAmount\\\": -100, \\\"contractStatus\\\": \\\"审批中\\\"}}\"}"
        },
        "url": {"raw": "http://localhost:8086/datamapping/config/test", "host": ["localhost"], "port": "8086", "path": ["datamapping", "config", "test"]}
      }
    },
    {
      "name": "分页查询配置",
      "request": {
        "method": "POST",
        "header": [{"key": "Content-Type", "value": "application/json"}],
        "body": {
          "mode": "raw",
          "raw": "{\"pageNum\": 1, \"pageSize\": 10, \"query\": {\"configName\": \"\", \"status\": 1}}"
        },
        "url": {"raw": "http://localhost:8086/datamapping/config/page", "host": ["localhost"], "port": "8086", "path": ["datamapping", "config", "page"]}
      }
    },
    {
      "name": "新建配置",
      "request": {
        "method": "POST",
        "header": [{"key": "Content-Type", "value": "application/json"}],
        "body": {
          "mode": "raw",
          "raw": "{\"configName\": \"测试配置\", \"configCode\": \"TEST_001\", \"systemId\": \"SYS_001\", \"systemName\": \"测试系统\", \"configVersion\": \"1.0\", \"apiVersion\": \"v1.0\", \"targetEndpoint\": \"/api/test\", \"jsonRootPath\": \"$.data\", \"configJson\": \"{}\", \"description\": \"测试配置\", \"createUserId\": \"admin\"}"
        },
        "url": {"raw": "http://localhost:8086/datamapping/config/create", "host": ["localhost"], "port": "8086", "path": ["datamapping", "config", "create"]}
      }
    }
  ]
}
```

---

## 八、执行测试

### 8.1 运行测试类
```bash
cd c:\works\project\ruoyi-vue-pro-self-demo\gnosis-project\gnosis-datamapping
mvn test -Dtest=Scenario1DirectMappingTest
mvn test -Dtest=Scenario2And3CrossNodeMappingTest
mvn test -Dtest=Scenario4ListExtractionTest
mvn test -Dtest=Scenario5StringToListTest
```

### 8.2 运行单元测试
```bash
mvn test -Dtest=MappingEngineTest
mvn test -Dtest=ValidationEngineTest
mvn test -Dtest=TransformEngineTest
```

### 8.3 访问Swagger文档
打开浏览器访问：`http://localhost:8086/swagger-ui.html`

---

## 九、测试验证清单

| 场景 | 测试项 | 预期结果 | 状态 |
|------|--------|----------|------|
| 场景1 | 正常转换（trim+uppercase+lookup） | 转换成功，数据正确 | ☑️ |
| 场景1 | 校验失败（pattern+min+enum） | 返回validationErrors | ☑️ |
| 场景1 | 类型转换（'1'→Integer） | 转换成功 | ☑️ |
| 场景1 | 类型转换失败（'a'→Integer） | 降级为原值，记录错误 | ☑️ |
| 场景1 | 必填校验失败 | 返回validationErrors | ☑️ |
| 场景2 | 根节点→指定节点（单层） | 数据写入basicInfo节点 | ☑️ |
| 场景2 | 根节点→指定节点（多层） | 数据写入paymentInfo.amount | ☑️ |
| 场景3 | 指定节点→根节点 | 深层字段提取到根节点 | ☑️ |
| 场景3 | 数组元素→根节点 | parties[0].partyName→principalParty | ☑️ |
| 场景4 | 按索引提取（index=0） | 取第一条数据 | ☑️ |
| 场景4 | 按条件筛选 | 筛选partyType==甲方的数据 | ☑️ |
| 场景4 | 值拼接（separator="、"） | 多个值按分隔符拼接 | ☑️ |
| 场景4 | 格式化拼接 | 按模板{partyName}({partyType})拼接 | ☑️ |
| 场景5 | String转List（separator=","） | 字符串拆分为数组 | ☑️ |
| 场景5 | 链式规则（enrichListItems） | 数组项补充额外字段 | ☑️ |
| 场景5 | 链式规则（transformListItems） | 数组项应用转换规则 | ☑️ |
| 场景5 | 未知数据降级 | 使用defaultValues填充 | ☑️ |
