我现在想开发一个统一消息通知的组件，适用于系统内消息的统一管理（短信消息、邮件消息等），具有丰富的消息模板管理能力（纯文本、富文本、附件等）。另外还需要所有消息发送的全链路管理能力，日志能力等
新建模块 gnosis-notice-center。严格按照技术栈要求 。完成前后端完整功能开发，前端代码写在 gnosis-notice-center frontend下。后端需要创建方便简单可供其他模块调用的api类。
在完成任务之前，验证前后端正常编译，API功能都正常，前端功能都正常，符合需求希望。