# 分布式队列系统 REST API 示例

@baseUrl = http://localhost:8080
@contentType = application/json
@createdAt = {{$datetime iso8601}}
@requestId = {{$guid}}

###

# 📋 文档概述
本文档展示了分布式队列系统的所有可用API接口，涵盖任务管理、队列实例管理和配置管理三大核心功能。
按照 VS Code REST Client 插件格式编写，支持变量定义和复用。

# 📦 1. 任务管理相关接口

### 接口概览
任务管理接口提供异步和同步两种任务提交方式，以及队列状态查询和健康检查功能。

### 1.1 🔧 异步提交任务
POST {{baseUrl}}/distribute-queue/tasks/{{queueName}}
Content-Type: {{contentType}}
X-Request-ID: {{requestId}}
X-Timestamp: {{createdAt}}


{
  "payload": "{\"businessType\":\"email\",\"data\":{\"to\":\"user@example.com\",\"subject\":\"测试邮件\",\"content\":\"这是一封测试邮件\"}}"
}

###

### 1.2 ⚡ 同步提交任务并等待结果
POST {{baseUrl}}/distribute-queue/tasks-sync/{{queueName}}?timeoutMs=5000
Content-Type: {{contentType}}
X-Request-ID: {{requestId}}
X-Timestamp: {{createdAt}}

**请求示例:**
```json
{
  "payload": "{\"businessType\":\"email\",\"data\":{\"to\":\"user@example.com\",\"subject\":\"测试邮件\",\"content\":\"这是一封测试邮件\"}}"
}
```

**响应示例:**
```json
{
  "status": "success",
  "message": "任务已加入队列",
  "data": {
    "queueName": "email-queue"
  },
  "timestamp": 1707582254123
}
```


{
  "payload": "{\"businessType\":\"notification\",\"data\":{\"userId\":12345,\"message\":\"订单已发货\"}}"
}

###

### 1.3 📊 查询队列状态
GET {{baseUrl}}/distribute-queue/queues/{{queueName}}/status
Accept: {{contentType}}
X-Request-ID: {{requestId}}

**请求示例:**
```json
{
  "payload": "{\"businessType\":\"notification\",\"data\":{\"userId\":12345,\"message\":\"订单已发货\"}}"
}
```

**成功响应示例:**
```json
{
  "status": "success",
  "message": "任务处理完成",
  "data": {
    "requestId": "abcd1234efgh5678",
    "result": "{\"status\":\"sent\",\"messageId\":\"msg_001\"}"
  },
  "timestamp": 1707582254123
}
```

**超时响应示例:**
```json
{
  "status": "error",
  "message": "等待结果超时: 5000ms",
  "data": {
    "requestId": "abcd1234efgh5678"
  },
  "timestamp": 1707582254123
}
```


###

### 1.4 🏥 健康检查
GET {{baseUrl}}/distribute-queue/health
Accept: {{contentType}}

**响应示例:**
```json
{
  "status": "success",
  "message": "获取队列状态成功",
  "data": {
    "queueName": "email-queue",
    "timestamp": 1707582254123
  },
  "timestamp": 1707582254123
}
```


###

**响应示例:**
```json
{
  "status": "success",
  "message": "服务健康",
  "data": {
    "status": "UP",
    "service": "分布式队列服务",
    "timestamp": 1707582254123
  },
  "timestamp": 1707582254123
}
```

## 🏗️ 2. 队列实例管理接口

### 接口概览
队列实例管理接口支持动态创建、配置、查询和删除队列实例，实现多队列隔离管理。

### 2.1 🆕 创建队列实例
**POST** `/distribute-queue/admin/queue-instances/{queueName}`

动态创建新的队列实例，支持自定义配置参数。

**请求示例:**
```json
{
  "maxLength": 1000,
  "maxQps": 100,
  "pollInterval": 1000
}
```

**响应示例:**
```json
{
  "status": "success",
  "message": "队列实例创建成功",
  "data": {
    "queueName": "sms-queue",
    "config": {
      "maxLength": 1000,
      "maxQps": 100,
      "pollInterval": 1000
    }
  },
  "timestamp": 1707582254123
}
```

### 2.2 🔍 获取队列实例信息
**GET** `/distribute-queue/admin/queue-instances/{queueName}`

查询指定队列实例的详细配置和运行信息。

**响应示例:**
```json
{
  "status": "success",
  "message": "获取队列信息成功",
  "data": {
    "queueName": "sms-queue",
    "config": {
      "maxLength": 1000,
      "maxQps": 100,
      "pollInterval": 1000
    },
    "status": "active"
  },
  "timestamp": 1707582254123
}
```

### 2.3 📋 获取所有队列实例
**GET** `/distribute-queue/admin/queue-instances`

获取系统中所有队列实例的统计信息和配置概览。

**响应示例:**
```json
{
  "status": "success",
  "message": "获取所有队列实例成功",
  "data": {
    "totalInstances": 3,
    "queueNames": ["email-queue", "sms-queue", "notification-queue"],
    "configurations": {
      "email-queue": {
        "maxLength": 1000,
        "maxQps": 100,
        "pollInterval": 1000
      },
      "sms-queue": {
        "maxLength": 500,
        "maxQps": 50,
        "pollInterval": 2000
      }
    }
  },
  "timestamp": 1707582254123
}
```

### 2.4 🛠️ 更新队列配置
**PUT** `/distribute-queue/admin/queue-instances/{queueName}/config`

动态更新队列实例的运行配置，无需重启服务。

**请求示例:**
```json
{
  "maxLength": 2000,
  "maxQps": 200,
  "pollInterval": 500
}
```

**响应示例:**
```json
{
  "status": "success",
  "message": "配置更新成功",
  "data": {
    "queueName": "sms-queue",
    "updatedConfig": {
      "maxLength": 2000,
      "maxQps": 200,
      "pollInterval": 500
    }
  },
  "timestamp": 1707582254123
}
```

### 2.5 🗑️ 删除队列实例
**DELETE** `/distribute-queue/admin/queue-instances/{queueName}`

安全删除指定的队列实例，会停止相关消费者和服务。

**响应示例:**
```json
{
  "status": "success",
  "message": "队列实例删除成功",
  "data": {
    "queueName": "temp-queue"
  },
  "timestamp": 1707582254123
}
```

### 2.6 📦 批量创建队列实例
**POST** `/distribute-queue/admin/queue-instances/batch`

一次性创建多个队列实例，提高配置效率。

**请求示例:**
```json
{
  "email-queue": {
    "maxLength": 1000,
    "maxQps": 100,
    "pollInterval": 1000
  },
  "sms-queue": {
    "maxLength": 500,
    "maxQps": 50,
    "pollInterval": 2000
  },
  "notification-queue": {
    "maxLength": 800,
    "maxQps": 80,
    "pollInterval": 1500
  }
}
```

**响应示例:**
```json
{
  "status": "success",
  "message": "批量队列实例创建成功",
  "data": {
    "createdQueues": ["email-queue", "sms-queue", "notification-queue"]
  },
  "timestamp": 1707582254123
}
```

## ⚙️ 3. 队列配置管理接口

### 接口概览
配置管理接口提供细粒度的队列参数调整能力，支持运行时动态配置。

### 3.1 📋 获取所有队列配置
**GET** `/distribute-queue/admin/queue-config`

获取系统中所有队列的当前配置信息。

**响应示例:**
```json
{
  "status": "success",
  "message": "获取所有配置成功",
  "data": {
    "email-queue": {
      "maxLength": 1000,
      "maxQps": 100
    },
    "sms-queue": {
      "maxLength": 500,
      "maxQps": 50
    }
  },
  "timestamp": 1707582254123
}
```

### 3.2 🔍 获取指定队列配置
**GET** `/distribute-queue/admin/queue-config/{queueName}`

获取特定队列的详细配置参数。

**响应示例:**
```json
{
  "status": "success",
  "message": "获取队列配置成功",
  "data": {
    "queueName": "email-queue",
    "maxLength": 1000,
    "maxQps": 100
  },
  "timestamp": 1707582254123
}
```

### 3.3 📏 设置队列最大长度
**POST** `/distribute-queue/admin/queue-config/{queueName}/max-length?maxLength=1500`

设置队列的最大容量限制，防止内存溢出。

**响应示例:**
```json
{
  "status": "success",
  "message": "队列 'email-queue' 最大长度设置为 1500",
  "timestamp": 1707582254123
}
```

### 3.4 🚀 设置队列最大QPS
**POST** `/distribute-queue/admin/queue-config/{queueName}/max-qps?maxQps=150`

控制队列每秒最大处理请求数，实现流量控制。

**响应示例:**
```json
{
  "status": "success",
  "message": "队列 'email-queue' 最大QPS设置为 150",
  "timestamp": 1707582254123
}
```

### 3.5 ⏱️ 设置空队列轮询间隔
**POST** `/distribute-queue/admin/queue-config/empty-poll-interval?intervalMs=2000`

调整空队列时的轮询间隔，平衡性能和资源消耗。

**响应示例:**
```json
{
  "status": "success",
  "message": "空队列轮询间隔设置为 2000 ms",
  "timestamp": 1707582254123
}
```

### 3.6 🔄 重置队列配置
**DELETE** `/distribute-queue/admin/queue-config/{queueName}`

将指定队列的配置重置为系统默认值。

**响应示例:**
```json
{
  "status": "success",
  "message": "队列 'email-queue' 配置已重置为默认值",
  "timestamp": 1707582254123
}
```

### 3.7 📦 批量设置配置
**POST** `/distribute-queue/admin/queue-config/batch`

批量更新多个队列的配置参数，提高运维效率。

**请求示例:**
```json
{
  "email-queue": {
    "maxLength": 2000,
    "maxQps": 200
  },
  "sms-queue": {
    "maxLength": 1000,
    "maxQps": 100
  }
}
```

**响应示例:**
```json
{
  "status": "success",
  "message": "批量配置更新: 设置 email-queue 最大长度为 2000; 设置 email-queue 最大QPS为 200; 设置 sms-queue 最大长度为 1000; 设置 sms-queue 最大QPS为 100; ",
  "timestamp": 1707582254123
}
```

## ❌ 4. 错误响应示例

## 错误响应示例
系统使用标准HTTP状态码配合详细的错误信息，便于问题定位和处理。

### 5.1 🚫 队列不存在 (404)
错误码: QUEUE_NOT_FOUND
GET {{baseUrl}}/distribute-queue/admin/queue-instances/non-existent-queue
Accept: {{contentType}}

###

### 5.2 📦 队列已满 (429)
错误码: QUEUE_FULL
POST {{baseUrl}}/distribute-queue/tasks/full-queue
Content-Type: {{contentType}}

{
  "payload": "{\"test\":\"data\"}"
}

###

### 5.3 ⚠️ 参数验证错误 (400)
错误码: VALIDATION_ERROR
POST {{baseUrl}}/distribute-queue/admin/queue-config/invalid-queue/max-length?maxLength=-1

###

### 5.4 💥 系统内部错误 (500)
错误码: INTERNAL_ERROR
GET {{baseUrl}}/distribute-queue/health
Accept: {{contentType}}
X-Custom-Header: invalid-value

###

> 📌 **注意**: 以上示例均基于开发环境配置，生产环境请注意安全认证和访问控制。
> 建议在实际使用时添加适当的认证头和安全措施。