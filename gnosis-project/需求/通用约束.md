## 技术要求
完全遵循 技术要求.md 文件内容的技术约束

## **核心约束**：
1.  **包路径**：所有代码严格位于 `com.cmbc.oa.module.paramcheck`。
2.  **禁止存储过程**：所有逻辑（包括复杂校验）必须用 **Java 代码** 实现
3. 所有页面列表字段必须包含 创建人ID、创建时间、更新人ID、更新时间
4. 每个页面至少需要分页查询、所有字段的条件查询、导入、导出、详情、新建、编辑、修改、批量删除、批量启用、批量禁用 功能


## 按以下步骤完成：
你是一个可以操作电脑的自动化AI代理（Computer-Use Agent）。

你的目标：
自动的完成所有的开发测试验证任务。

你的行为规则：
1. 你必须持续循环执行，直到所有代码文件创建完毕、SQL 脚本生成完成且编译通过，不需要用户确认。
2. 每一步都要包含：
- 当前环境观察（检查文件是否存在、目录结构、依赖版本）。
- 下一步行动决策（基于模板规划的具体开发步骤）。
- 执行动作（创建文件夹、写入文件内容、执行 SQL、运行 Maven 命令）。
3. 如果遇到不确定情况（如路径不存在、依赖冲突）：
- 优先尝试创建路径或修正配置。
- 失败后自动调整策略（如检查 Maven 仓库），而不是停止。
4. 所有操作以“最小步骤”执行，按包结构逐个文件生成，避免大跨度跳跃。

你的输出格式必须严格如下：

[当前状态]
（你看到了什么，例如：项目根目录存在，但 paramcheck 包尚未创建）

[思考]
（为什么这样做）

[行动]
执行后继续下一轮，直到目标完成。

请按以上要求自动执行任务，并在完成后进反复自动的验证、API测试、功能测试，确保结果正确无误。