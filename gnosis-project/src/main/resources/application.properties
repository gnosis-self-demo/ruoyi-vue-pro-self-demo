# 应用基本信息
spring.application.name=distributed-queue-service
server.port=8080

# ========================
# = Database Configuration =
# ========================
spring.datasource.url=jdbc:opengauss://localserver.gnosis:5432/gnosis_sample?currentSchema=gnosis_sample
spring.datasource.username=gnosis_sample
spring.datasource.password=Gnosis@123
spring.datasource.driver-class-name=org.postgresql.Driver

# ========================
# = MyBatis Plus Configuration =
# ========================
mybatis-plus.mapper-locations=classpath*:mapper/**/*.xml
mybatis-plus.type-aliases-package=gnosis.sample.distribute.queue.entity
mybatis-plus.configuration.map-underscore-to-camel-case=true
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# ========================
# = LiteFlow Configuration =
# ========================
liteflow.rule-source=sql
liteflow.rule-source-ext-type=sql
liteflow.sql-rule-table=liteflow_sys_rule
liteflow.sql-rule-field-chain-name=chain_name
liteflow.sql-rule-field-content=rule_content
liteflow.sql-rule-field-type=rule_type
liteflow.sql-rule-field-enabled=enabled
liteflow.sql-datasource-ref=spring.datasource

# ========================
# = Logging Configuration =
# ========================
logging.level.root=INFO
logging.level.gnosis.sample.distribute.queue=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=logs/distributed-queue.log
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=30

# ========================
# = Web Configuration =
# ========================
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# ========================
# = JSON Configuration =
# ========================
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=GMT+8
spring.jackson.default-property-inclusion=NON_NULL

# ========================
# = Task Execution Configuration =
# ========================
spring.task.execution.pool.core-size=10
spring.task.execution.pool.max-size=50
spring.task.execution.pool.queue-capacity=100
spring.task.execution.thread-name-prefix=queue-task-

# ========================
# = Task Scheduling Configuration =
# ========================
spring.task.scheduling.pool.size=5
spring.task.scheduling.thread-name-prefix=scheduler-

# ========================
# = Actuator Configuration =
# ========================
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# ========================
# = Distributed Queue Configuration =
# ========================
distributed.queue.sms.maxLength=1000
distributed.queue.sms.maxQps=100
distributed.queue.sms.pollInterval=500

distributed.queue.email.maxLength=500
distributed.queue.email.maxQps=50
distributed.queue.email.pollInterval=1000

distributed.queue.notification.maxLength=2000
distributed.queue.notification.maxQps=200
distributed.queue.notification.pollInterval=100