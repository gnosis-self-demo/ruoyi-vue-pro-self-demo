<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lifecycle.repository.LifecycleDownstreamConfigMapper">

    <resultMap id="BaseResultMap" type="lifecycle.domain.LifecycleDownstreamConfig">
        <id column="id" property="id"/>
        <result column="final_state_id" property="finalStateId"/>
        <result column="downstream_business_type" property="downstreamBusinessType"/>
        <result column="trigger_condition" property="triggerCondition"/>
        <result column="trigger_mode" property="triggerMode"/>
        <result column="config_data" property="configData"/>
        <result column="is_active" property="isActive"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, final_state_id, downstream_business_type, trigger_condition, trigger_mode, config_data, is_active,
        create_user_id, update_user_id, create_time, update_time
    </sql>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM lifecycle_downstream_config
        WHERE id = #{id}
    </select>

    <select id="selectByFinalStateId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM lifecycle_downstream_config
        WHERE final_state_id = #{finalStateId}
        ORDER BY is_active DESC, create_time DESC
    </select>

    <select id="selectList" parameterType="lifecycle.domain.LifecycleDownstreamConfig" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM lifecycle_downstream_config
        <where>
            <if test="finalStateId != null and finalStateId != ''">
                AND final_state_id = #{finalStateId}
            </if>
            <if test="downstreamBusinessType != null and downstreamBusinessType != ''">
                AND downstream_business_type = #{downstreamBusinessType}
            </if>
            <if test="triggerMode != null and triggerMode != ''">
                AND trigger_mode = #{triggerMode}
            </if>
            <if test="isActive != null">
                AND is_active = #{isActive}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="lifecycle.domain.LifecycleDownstreamConfig">
        INSERT INTO lifecycle_downstream_config (
            id, final_state_id, downstream_business_type, trigger_condition, trigger_mode,
            config_data, is_active,
            create_user_id, update_user_id, create_time, update_time
        ) VALUES (
            #{id}, #{finalStateId}, #{downstreamBusinessType}, #{triggerCondition}, #{triggerMode},
            #{configData}, #{isActive},
            #{createUserId}, #{updateUserId}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="lifecycle.domain.LifecycleDownstreamConfig">
        UPDATE lifecycle_downstream_config
        <set>
            <if test="downstreamBusinessType != null and downstreamBusinessType != ''">
                downstream_business_type = #{downstreamBusinessType},
            </if>
            <if test="triggerCondition != null">
                trigger_condition = #{triggerCondition},
            </if>
            <if test="triggerMode != null and triggerMode != ''">
                trigger_mode = #{triggerMode},
            </if>
            <if test="configData != null">
                config_data = #{configData},
            </if>
            <if test="isActive != null">
                is_active = #{isActive},
            </if>
            <if test="updateUserId != null and updateUserId != ''">
                update_user_id = #{updateUserId},
            </if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM lifecycle_downstream_config WHERE id = #{id}
    </delete>

    <delete id="deleteByIds" parameterType="java.lang.String">
        DELETE FROM lifecycle_downstream_config WHERE id IN
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
