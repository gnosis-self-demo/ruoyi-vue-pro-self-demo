<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gnosis.signature.mapper.SignatureCertificateMapper">

    <resultMap id="BaseResultMap" type="com.gnosis.signature.domain.SignatureCertificate">
        <id column="id" property="id"/>
        <result column="cert_code" property="certCode"/>
        <result column="cert_name" property="certName"/>
        <result column="cert_type" property="certType"/>
        <result column="ca_org" property="caOrg"/>
        <result column="cert_sn" property="certSn"/>
        <result column="cert_content" property="certContent"/>
        <result column="expire_time" property="expireTime"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, cert_code, cert_name, cert_type, ca_org, cert_sn, cert_content,
        expire_time, description, status, create_user_id, update_user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.gnosis.signature.domain.SignatureCertificate">
        INSERT INTO sig_certificate (
            id, cert_code, cert_name, cert_type, ca_org, cert_sn, cert_content,
            expire_time, description, status, create_user_id, update_user_id, create_time, update_time
        ) VALUES (
            #{id}, #{certCode}, #{certName}, #{certType}, #{caOrg}, #{certSn}, #{certContent},
            #{expireTime}, #{description}, #{status}, #{createUserId}, #{updateUserId}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.gnosis.signature.domain.SignatureCertificate">
        UPDATE sig_certificate
        <set>
            <if test="certCode != null and certCode != ''">cert_code = #{certCode},</if>
            <if test="certName != null and certName != ''">cert_name = #{certName},</if>
            <if test="certType != null and certType != ''">cert_type = #{certType},</if>
            <if test="caOrg != null">ca_org = #{caOrg},</if>
            <if test="certSn != null">cert_sn = #{certSn},</if>
            <if test="certContent != null">cert_content = #{certContent},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateUserId != null and updateUserId != ''">update_user_id = #{updateUserId},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sig_certificate WHERE id = #{id}
    </select>

    <select id="selectByCondition" parameterType="com.gnosis.signature.dto.certificate.SignatureCertificateQueryRequest" resultMap="BaseResultMap">
        <bind name="offset" value="(pageNum != null and pageNum > 0 ? pageNum - 1 : 0) * (pageSize != null and pageSize > 0 ? pageSize : 10)" />
        SELECT <include refid="Base_Column_List"/> FROM sig_certificate
        <where>
            <if test="certCode != null and certCode != ''">AND cert_code = #{certCode}</if>
            <if test="certName != null and certName != ''">AND cert_name LIKE CONCAT('%', #{certName}, '%')</if>
            <if test="certType != null and certType != ''">AND cert_type = #{certType}</if>
            <if test="caOrg != null and caOrg != ''">AND ca_org LIKE CONCAT('%', #{caOrg}, '%')</if>
            <if test="certSn != null and certSn != ''">AND cert_sn = #{certSn}</if>
            <if test="expireTime != null">AND expire_time &lt;= #{expireTime}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY expire_time ASC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByCondition" parameterType="com.gnosis.signature.dto.certificate.SignatureCertificateQueryRequest" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sig_certificate
        <where>
            <if test="certCode != null and certCode != ''">AND cert_code = #{certCode}</if>
            <if test="certName != null and certName != ''">AND cert_name LIKE CONCAT('%', #{certName}, '%')</if>
            <if test="certType != null and certType != ''">AND cert_type = #{certType}</if>
            <if test="caOrg != null and caOrg != ''">AND ca_org LIKE CONCAT('%', #{caOrg}, '%')</if>
            <if test="certSn != null and certSn != ''">AND cert_sn = #{certSn}</if>
            <if test="expireTime != null">AND expire_time &lt;= #{expireTime}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM sig_certificate WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sig_certificate WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>

    <update id="batchUpdateStatus">
        UPDATE sig_certificate SET status = #{status}, update_time = CURRENT_TIMESTAMP WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sig_certificate WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

</mapper>
