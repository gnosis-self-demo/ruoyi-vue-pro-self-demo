<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gnosis.signature.mapper.SignatureProcessMapper">

    <resultMap id="BaseResultMap" type="com.gnosis.signature.domain.SignatureProcess">
        <id column="id" property="id"/>
        <result column="process_code" property="processCode"/>
        <result column="process_name" property="processName"/>
        <result column="process_type" property="processType"/>
        <result column="process_config" property="processConfig"/>
        <result column="sign_order" property="signOrder"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, process_code, process_name, process_type, process_config, sign_order,
        description, status, create_user_id, update_user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.gnosis.signature.domain.SignatureProcess">
        INSERT INTO sig_process (
            id, process_code, process_name, process_type, process_config, sign_order,
            description, status, create_user_id, update_user_id, create_time, update_time
        ) VALUES (
            #{id}, #{processCode}, #{processName}, #{processType}, #{processConfig}, #{signOrder},
            #{description}, #{status}, #{createUserId}, #{updateUserId}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.gnosis.signature.domain.SignatureProcess">
        UPDATE sig_process
        <set>
            <if test="processCode != null and processCode != ''">process_code = #{processCode},</if>
            <if test="processName != null and processName != ''">process_name = #{processName},</if>
            <if test="processType != null and processType != ''">process_type = #{processType},</if>
            <if test="processConfig != null">process_config = #{processConfig},</if>
            <if test="signOrder != null">sign_order = #{signOrder},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateUserId != null and updateUserId != ''">update_user_id = #{updateUserId},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sig_process WHERE id = #{id}
    </select>

    <select id="selectByCondition" parameterType="com.gnosis.signature.dto.process.SignatureProcessQueryRequest" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sig_process
        <where>
            <if test="processCode != null and processCode != ''">AND process_code = #{processCode}</if>
            <if test="processName != null and processName != ''">AND process_name LIKE CONCAT('%', #{processName}, '%')</if>
            <if test="processType != null and processType != ''">AND process_type = #{processType}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY sign_order ASC, create_time DESC
    </select>

    <select id="countByCondition" parameterType="com.gnosis.signature.dto.process.SignatureProcessQueryRequest" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sig_process
        <where>
            <if test="processCode != null and processCode != ''">AND process_code = #{processCode}</if>
            <if test="processName != null and processName != ''">AND process_name LIKE CONCAT('%', #{processName}, '%')</if>
            <if test="processType != null and processType != ''">AND process_type = #{processType}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM sig_process WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sig_process WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>

    <update id="batchUpdateStatus">
        UPDATE sig_process SET status = #{status}, update_time = CURRENT_TIMESTAMP WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>

</mapper>
