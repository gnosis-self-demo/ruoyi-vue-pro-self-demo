<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gnosis.notice.mapper.NoticeBlacklistMapper">

    <resultMap id="BaseResultMap" type="com.gnosis.notice.domain.NoticeBlacklist">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="notice_type" property="noticeType"/>
        <result column="template_code" property="templateCode"/>
        <result column="reason" property="reason"/>
        <result column="status" property="status"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, notice_type, template_code, reason, status,
        create_user_id, update_user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.gnosis.notice.domain.NoticeBlacklist">
        INSERT INTO sys_notice_blacklist (
            id, user_id, notice_type, template_code, reason, status,
            create_user_id, update_user_id, create_time, update_time
        ) VALUES (
            #{id}, #{userId}, #{noticeType}, #{templateCode}, #{reason}, #{status},
            #{createUserId}, #{updateUserId}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.gnosis.notice.domain.NoticeBlacklist">
        UPDATE sys_notice_blacklist
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="noticeType != null">notice_type = #{noticeType},</if>
            <if test="templateCode != null">template_code = #{templateCode},</if>
            <if test="reason != null">reason = #{reason},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateUserId != null">update_user_id = #{updateUserId},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_notice_blacklist
        WHERE id = #{id}
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap" parameterType="com.gnosis.notice.dto.blacklist.NoticeBlacklistQueryRequest">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_notice_blacklist
        <where>
            <if test="userId != null and userId != ''">AND user_id = #{userId}</if>
            <if test="noticeType != null and noticeType != ''">AND notice_type = #{noticeType}</if>
            <if test="templateCode != null and templateCode != ''">AND template_code = #{templateCode}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
        <if test="pageNum != null and pageSize != null">
            LIMIT #{pageSize} OFFSET #{pageNum}
        </if>
    </select>

    <select id="countByCondition" resultType="java.lang.Long" parameterType="com.gnosis.notice.dto.blacklist.NoticeBlacklistQueryRequest">
        SELECT COUNT(*) FROM sys_notice_blacklist
        <where>
            <if test="userId != null and userId != ''">AND user_id = #{userId}</if>
            <if test="noticeType != null and noticeType != ''">AND notice_type = #{noticeType}</if>
            <if test="templateCode != null and templateCode != ''">AND template_code = #{templateCode}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <delete id="deleteById">
        DELETE FROM sys_notice_blacklist WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sys_notice_blacklist WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="batchUpdateStatus">
        UPDATE sys_notice_blacklist
        SET status = #{status}, update_time = CURRENT_TIMESTAMP
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <select id="checkBlacklist" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM sys_notice_blacklist
        WHERE user_id = #{userId}
          AND notice_type = #{noticeType}
          AND status = 1
          AND (template_code = #{templateCode} OR template_code IS NULL)
    </select>

</mapper>
