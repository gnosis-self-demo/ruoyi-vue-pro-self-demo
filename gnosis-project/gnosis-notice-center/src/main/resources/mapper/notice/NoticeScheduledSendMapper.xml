<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gnosis.notice.mapper.NoticeScheduledSendMapper">

    <resultMap id="BaseResultMap" type="com.gnosis.notice.domain.NoticeScheduledSend">
        <id column="id" property="id"/>
        <result column="template_code" property="templateCode"/>
        <result column="notice_type" property="noticeType"/>
        <result column="receiver" property="receiver"/>
        <result column="subject" property="subject"/>
        <result column="content" property="content"/>
        <result column="params" property="params"/>
        <result column="attachment_paths" property="attachmentPaths"/>
        <result column="priority" property="priority"/>
        <result column="group_id" property="groupId"/>
        <result column="scheduled_time" property="scheduledTime"/>
        <result column="send_status" property="sendStatus"/>
        <result column="log_id" property="logId"/>
        <result column="error_msg" property="errorMsg"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, template_code, notice_type, receiver, subject, content, params, attachment_paths,
        priority, group_id, scheduled_time, send_status, log_id, error_msg,
        create_user_id, update_user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.gnosis.notice.domain.NoticeScheduledSend">
        INSERT INTO sys_notice_scheduled_send (
            id, template_code, notice_type, receiver, subject, content, params, attachment_paths,
            priority, group_id, scheduled_time, send_status, log_id, error_msg,
            create_user_id, update_user_id, create_time, update_time
        ) VALUES (
            #{id}, #{templateCode}, #{noticeType}, #{receiver}, #{subject}, #{content}, #{params}, #{attachmentPaths},
            #{priority}, #{groupId}, #{scheduledTime}, #{sendStatus}, #{logId}, #{errorMsg},
            #{createUserId}, #{updateUserId}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.gnosis.notice.domain.NoticeScheduledSend">
        UPDATE sys_notice_scheduled_send
        <set>
            <if test="templateCode != null">template_code = #{templateCode},</if>
            <if test="noticeType != null">notice_type = #{noticeType},</if>
            <if test="receiver != null">receiver = #{receiver},</if>
            <if test="subject != null">subject = #{subject},</if>
            <if test="content != null">content = #{content},</if>
            <if test="params != null">params = #{params},</if>
            <if test="attachmentPaths != null">attachment_paths = #{attachmentPaths},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="groupId != null">group_id = #{groupId},</if>
            <if test="scheduledTime != null">scheduled_time = #{scheduledTime},</if>
            <if test="sendStatus != null">send_status = #{sendStatus},</if>
            <if test="logId != null">log_id = #{logId},</if>
            <if test="errorMsg != null">error_msg = #{errorMsg},</if>
            <if test="updateUserId != null">update_user_id = #{updateUserId},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_notice_scheduled_send
        WHERE id = #{id}
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap" parameterType="com.gnosis.notice.dto.scheduledsend.NoticeScheduledSendQueryRequest">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_notice_scheduled_send
        <where>
            <if test="noticeType != null and noticeType != ''">AND notice_type = #{noticeType}</if>
            <if test="receiver != null and receiver != ''">AND receiver = #{receiver}</if>
            <if test="sendStatus != null">AND send_status = #{sendStatus}</if>
            <if test="startTime != null">AND scheduled_time &gt;= #{startTime}</if>
            <if test="endTime != null">AND scheduled_time &lt;= #{endTime}</if>
        </where>
        ORDER BY scheduled_time DESC
        <if test="pageNum != null and pageSize != null">
            LIMIT #{pageSize} OFFSET #{pageNum}
        </if>
    </select>

    <select id="countByCondition" resultType="java.lang.Long" parameterType="com.gnosis.notice.dto.scheduledsend.NoticeScheduledSendQueryRequest">
        SELECT COUNT(*) FROM sys_notice_scheduled_send
        <where>
            <if test="noticeType != null and noticeType != ''">AND notice_type = #{noticeType}</if>
            <if test="receiver != null and receiver != ''">AND receiver = #{receiver}</if>
            <if test="sendStatus != null">AND send_status = #{sendStatus}</if>
            <if test="startTime != null">AND scheduled_time &gt;= #{startTime}</if>
            <if test="endTime != null">AND scheduled_time &lt;= #{endTime}</if>
        </where>
    </select>

    <delete id="deleteById">
        DELETE FROM sys_notice_scheduled_send WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sys_notice_scheduled_send WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectPendingList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_notice_scheduled_send
        WHERE send_status = 0 AND scheduled_time &lt;= CURRENT_TIMESTAMP
        ORDER BY scheduled_time ASC
        LIMIT 100
    </select>

</mapper>
