<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lifecycle.repository.LifecycleEventMetadataMapper">

    <resultMap id="BaseResultMap" type="com.gnosis.lifecycle.domain.LifecycleEventMetadata">
        <id column="id" property="id"/>
        <result column="event_type" property="eventType"/>
        <result column="metadata_key" property="metadataKey"/>
        <result column="metadata_type" property="metadataType"/>
        <result column="description" property="description"/>
        <result column="allowed_values" property="allowedValues"/>
        <result column="is_required" property="isRequired"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, event_type, metadata_key, metadata_type, description, allowed_values, is_required, sort_order,
        create_user_id, update_user_id, create_time, update_time
    </sql>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM lifecycle_event_metadata
        WHERE id = #{id}
    </select>

    <select id="selectByEventType" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM lifecycle_event_metadata
        WHERE event_type = #{eventType}
        ORDER BY sort_order ASC
    </select>

    <select id="selectList" parameterType="com.gnosis.lifecycle.domain.LifecycleEventMetadata" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM lifecycle_event_metadata
        <where>
            <if test="eventType != null and eventType != ''">
                AND event_type = #{eventType}
            </if>
            <if test="metadataKey != null and metadataKey != ''">
                AND metadata_key = #{metadataKey}
            </if>
            <if test="metadataType != null and metadataType != ''">
                AND metadata_type = #{metadataType}
            </if>
        </where>
        ORDER BY sort_order ASC, create_time DESC
    </select>

    <insert id="insert" parameterType="com.gnosis.lifecycle.domain.LifecycleEventMetadata">
        INSERT INTO lifecycle_event_metadata (
            id, event_type, metadata_key, metadata_type, description, allowed_values,
            is_required, sort_order,
            create_user_id, update_user_id, create_time, update_time
        ) VALUES (
            #{id}, #{eventType}, #{metadataKey}, #{metadataType}, #{description}, #{allowedValues},
            #{isRequired}, #{sortOrder},
            #{createUserId}, #{updateUserId}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.gnosis.lifecycle.domain.LifecycleEventMetadata">
        UPDATE lifecycle_event_metadata
        <set>
            <if test="eventType != null and eventType != ''">
                event_type = #{eventType},
            </if>
            <if test="metadataKey != null and metadataKey != ''">
                metadata_key = #{metadataKey},
            </if>
            <if test="metadataType != null and metadataType != ''">
                metadata_type = #{metadataType},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="allowedValues != null">
                allowed_values = #{allowedValues},
            </if>
            <if test="isRequired != null">
                is_required = #{isRequired},
            </if>
            <if test="sortOrder != null">
                sort_order = #{sortOrder},
            </if>
            <if test="updateUserId != null and updateUserId != ''">
                update_user_id = #{updateUserId},
            </if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM lifecycle_event_metadata WHERE id = #{id}
    </delete>

    <delete id="deleteByIds" parameterType="java.lang.String">
        DELETE FROM lifecycle_event_metadata WHERE id IN
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
