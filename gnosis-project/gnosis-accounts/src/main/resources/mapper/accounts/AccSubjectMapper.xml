<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gnosis.accounts.mapper.AccSubjectMapper">

    <resultMap id="BaseResultMap" type="com.gnosis.accounts.domain.AccSubject">
        <id column="id" property="id"/>
        <result column="subject_code" property="subjectCode"/>
        <result column="subject_name" property="subjectName"/>
        <result column="subject_type" property="subjectType"/>
        <result column="subject_category" property="subjectCategory"/>
        <result column="parent_id" property="parentId"/>
        <result column="level" property="level"/>
        <result column="balance_direction" property="balanceDirection"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, subject_code, subject_name, subject_type, subject_category, parent_id, level,
        balance_direction, description, status, create_user_id, update_user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.gnosis.accounts.domain.AccSubject">
        INSERT INTO sys_acc_subject (
            id, subject_code, subject_name, subject_type, subject_category, parent_id, level,
            balance_direction, description, status, create_user_id, update_user_id, create_time, update_time
        ) VALUES (
            #{id}, #{subjectCode}, #{subjectName}, #{subjectType}, #{subjectCategory}, #{parentId}, #{level},
            #{balanceDirection}, #{description}, #{status}, #{createUserId}, #{updateUserId}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.gnosis.accounts.domain.AccSubject">
        UPDATE sys_acc_subject
        <set>
            <if test="subjectCode != null and subjectCode != ''">subject_code = #{subjectCode},</if>
            <if test="subjectName != null and subjectName != ''">subject_name = #{subjectName},</if>
            <if test="subjectType != null and subjectType != ''">subject_type = #{subjectType},</if>
            <if test="subjectCategory != null and subjectCategory != ''">subject_category = #{subjectCategory},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="level != null">level = #{level},</if>
            <if test="balanceDirection != null and balanceDirection != ''">balance_direction = #{balanceDirection},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateUserId != null and updateUserId != ''">update_user_id = #{updateUserId},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_acc_subject WHERE id = #{id}
    </select>

    <select id="selectBySubjectCode" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_acc_subject WHERE subject_code = #{subjectCode}
    </select>

    <select id="selectByCondition" parameterType="com.gnosis.accounts.dto.subject.AccSubjectQueryRequest" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_acc_subject
        <where>
            <if test="subjectCode != null and subjectCode != ''">AND subject_code = #{subjectCode}</if>
            <if test="subjectName != null and subjectName != ''">AND subject_name LIKE CONCAT('%', #{subjectName}, '%')</if>
            <if test="subjectType != null and subjectType != ''">AND subject_type = #{subjectType}</if>
            <if test="subjectCategory != null and subjectCategory != ''">AND subject_category = #{subjectCategory}</if>
            <if test="parentId != null and parentId != ''">AND parent_id = #{parentId}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY subject_code ASC
    </select>

    <select id="countByCondition" parameterType="com.gnosis.accounts.dto.subject.AccSubjectQueryRequest" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_acc_subject
        <where>
            <if test="subjectCode != null and subjectCode != ''">AND subject_code = #{subjectCode}</if>
            <if test="subjectName != null and subjectName != ''">AND subject_name LIKE CONCAT('%', #{subjectName}, '%')</if>
            <if test="subjectType != null and subjectType != ''">AND subject_type = #{subjectType}</if>
            <if test="subjectCategory != null and subjectCategory != ''">AND subject_category = #{subjectCategory}</if>
            <if test="parentId != null and parentId != ''">AND parent_id = #{parentId}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM sys_acc_subject WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sys_acc_subject WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>

    <update id="batchUpdateStatus">
        UPDATE sys_acc_subject SET status = #{status}, update_time = CURRENT_TIMESTAMP WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>

</mapper>
