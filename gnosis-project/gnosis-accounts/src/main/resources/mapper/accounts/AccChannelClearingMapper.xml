<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gnosis.accounts.mapper.AccChannelClearingMapper">

    <resultMap id="BaseResultMap" type="com.gnosis.accounts.domain.AccChannelClearing">
        <id column="id" property="id"/>
        <result column="clearing_no" property="clearingNo"/>
        <result column="clearing_date" property="clearingDate"/>
        <result column="channel_code" property="channelCode"/>
        <result column="channel_name" property="channelName"/>
        <result column="clearing_type" property="clearingType"/>
        <result column="clearing_amount" property="clearingAmount"/>
        <result column="clearing_status" property="clearingStatus"/>
        <result column="journal_id" property="journalId"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, clearing_no, clearing_date, channel_code, channel_name, clearing_type, clearing_amount,
        clearing_status, journal_id, description, status, create_user_id, update_user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.gnosis.accounts.domain.AccChannelClearing">
        INSERT INTO sys_acc_channel_clearing (
            id, clearing_no, clearing_date, channel_code, channel_name, clearing_type, clearing_amount,
            clearing_status, journal_id, description, status, create_user_id, update_user_id, create_time, update_time
        ) VALUES (
            #{id}, #{clearingNo}, #{clearingDate}, #{channelCode}, #{channelName}, #{clearingType}, #{clearingAmount},
            #{clearingStatus}, #{journalId}, #{description}, #{status}, #{createUserId}, #{updateUserId}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.gnosis.accounts.domain.AccChannelClearing">
        UPDATE sys_acc_channel_clearing
        <set>
            <if test="clearingDate != null">clearing_date = #{clearingDate},</if>
            <if test="channelCode != null">channel_code = #{channelCode},</if>
            <if test="channelName != null">channel_name = #{channelName},</if>
            <if test="clearingType != null">clearing_type = #{clearingType},</if>
            <if test="clearingAmount != null">clearing_amount = #{clearingAmount},</if>
            <if test="clearingStatus != null and clearingStatus != ''">clearing_status = #{clearingStatus},</if>
            <if test="journalId != null">journal_id = #{journalId},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateUserId != null and updateUserId != ''">update_user_id = #{updateUserId},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_acc_channel_clearing WHERE id = #{id}
    </select>

    <select id="selectByCondition" parameterType="com.gnosis.accounts.dto.clearing.AccChannelClearingQueryRequest" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_acc_channel_clearing
        <where>
            <if test="clearingNo != null and clearingNo != ''">AND clearing_no = #{clearingNo}</if>
            <if test="channelCode != null and channelCode != ''">AND channel_code = #{channelCode}</if>
            <if test="clearingType != null and clearingType != ''">AND clearing_type = #{clearingType}</if>
            <if test="clearingStatus != null and clearingStatus != ''">AND clearing_status = #{clearingStatus}</if>
            <if test="clearingDateStart != null and clearingDateStart != ''">AND clearing_date &gt;= #{clearingDateStart}</if>
            <if test="clearingDateEnd != null and clearingDateEnd != ''">AND clearing_date &lt;= #{clearingDateEnd}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="countByCondition" parameterType="com.gnosis.accounts.dto.clearing.AccChannelClearingQueryRequest" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_acc_channel_clearing
        <where>
            <if test="clearingNo != null and clearingNo != ''">AND clearing_no = #{clearingNo}</if>
            <if test="channelCode != null and channelCode != ''">AND channel_code = #{channelCode}</if>
            <if test="clearingType != null and clearingType != ''">AND clearing_type = #{clearingType}</if>
            <if test="clearingStatus != null and clearingStatus != ''">AND clearing_status = #{clearingStatus}</if>
            <if test="clearingDateStart != null and clearingDateStart != ''">AND clearing_date &gt;= #{clearingDateStart}</if>
            <if test="clearingDateEnd != null and clearingDateEnd != ''">AND clearing_date &lt;= #{clearingDateEnd}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM sys_acc_channel_clearing WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sys_acc_channel_clearing WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>

    <update id="batchUpdateStatus">
        UPDATE sys_acc_channel_clearing SET status = #{status}, update_time = CURRENT_TIMESTAMP WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>

</mapper>
