/plan
按你是一个可以操作电脑的自动化AI代理（Computer-Use Agent）执行的任务，请使用以下提示进行任务规划。
按照 `c:\works\project\ruoyi-vue-pro-self-demo\gnosis-project\需求\business-lifecycle.md` 需求文档，
在本地项目 `C:\works\project\ruoyi-vue-pro-self-demo\gnosis-project` 中，
单独创建一个包，包名为：`paramcheck`。
拆分项目任务，按需调用其他智能体分别解决。
并在该包下完成完整的数据库脚本、Java 源码及单元测试、前端（React）工程等。

遵循如下规则： 
`c:\works\project\ruoyi-vue-pro-self-demo\gnosis-project\.trae\rules\tech-ness.md`
`c:\works\project\ruoyi-vue-pro-self-demo\gnosis-project\需求\技术要求.md`
`c:\works\project\ruoyi-vue-pro-self-demo\gnosis-project\需求\通用约束.md`
全自动进行开发、测试、验证，给我交付最终结果

你的行为规则：
1. 你必须持续循环执行，直到所有代码文件创建完毕、SQL 脚本生成完成且编译通过，不需要用户确认。
2. 每一步都要包含：
    - 当前环境观察（检查文件是否存在、目录结构、依赖版本）。
    - 下一步行动决策（基于模板规划的具体开发步骤）。
    - 执行动作（创建文件夹、写入文件内容、执行 SQL、运行 Maven 命令）。
3. 如果遇到不确定情况（如路径不存在、依赖冲突）：
    - 优先尝试创建路径或修正配置。
    - 失败后自动调整策略（如检查 Maven 仓库），而不是停止。
4. 所有操作以“最小步骤”执行，按包结构逐个文件生成，避免大跨度跳跃。

你的输出格式必须严格如下：

[当前状态]
（你看到了什么，例如：项目根目录存在）

[思考]
（为什么这样做，例如：首先需要建立基础目录结构和 Maven 依赖）

[行动]
（具体操作，例如：创建目录，写入文件内容等）
开发、测试、运行、前后端验证，不通过后再排查错误，修复开发，测试..重复循环执行。执行后继续下一轮，直到目标完成。